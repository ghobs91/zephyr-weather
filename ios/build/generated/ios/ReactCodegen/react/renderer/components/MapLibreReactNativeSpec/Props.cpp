
/**
 * This code was generated by [react-native-codegen](https://www.npmjs.com/package/react-native-codegen).
 *
 * Do not edit this file as changes may cause incorrect behavior and will be lost
 * once the code is regenerated.
 *
 * @generated by codegen project: GeneratePropsCpp.js
 */

#include <react/renderer/components/MapLibreReactNativeSpec/Props.h>
#include <folly/dynamic.h>
#include <react/renderer/core/PropsParserContext.h>
#include <react/renderer/core/propsConversions.h>

namespace facebook::react {

MLRNCalloutProps::MLRNCalloutProps(
    const PropsParserContext &context,
    const MLRNCalloutProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps)

     {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNCalloutProps::getDiffPropsImplementationTarget() const {
  return "MLRNCallout";
}

folly::dynamic MLRNCalloutProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNCalloutProps();
  const MLRNCalloutProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNCalloutProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  return result;
}
#endif
MLRNMarkerViewProps::MLRNMarkerViewProps(
    const PropsParserContext &context,
    const MLRNMarkerViewProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    id(convertRawProp(context, rawProps, "id", sourceProps.id, {})),
    lngLat(convertRawProp(context, rawProps, "lngLat", sourceProps.lngLat, {})),
    anchor(convertRawProp(context, rawProps, "anchor", sourceProps.anchor, {})),
    offset(convertRawProp(context, rawProps, "offset", sourceProps.offset, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNMarkerViewProps::getDiffPropsImplementationTarget() const {
  return "MLRNMarkerView";
}

folly::dynamic MLRNMarkerViewProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNMarkerViewProps();
  const MLRNMarkerViewProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNMarkerViewProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (id != oldProps->id) {
    result["id"] = id;
  }
    
  if (lngLat != oldProps->lngLat) {
    result["lngLat"] = lngLat;
  }
    
  if (anchor != oldProps->anchor) {
    result["anchor"] = toDynamic(anchor);
  }
    
  if (offset != oldProps->offset) {
    result["offset"] = toDynamic(offset);
  }
  return result;
}
#endif
MLRNPointAnnotationProps::MLRNPointAnnotationProps(
    const PropsParserContext &context,
    const MLRNPointAnnotationProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    id(convertRawProp(context, rawProps, "id", sourceProps.id, {})),
    title(convertRawProp(context, rawProps, "title", sourceProps.title, {})),
    snippet(convertRawProp(context, rawProps, "snippet", sourceProps.snippet, {})),
    selected(convertRawProp(context, rawProps, "selected", sourceProps.selected, {false})),
    draggable(convertRawProp(context, rawProps, "draggable", sourceProps.draggable, {false})),
    lngLat(convertRawProp(context, rawProps, "lngLat", sourceProps.lngLat, {})),
    anchor(convertRawProp(context, rawProps, "anchor", sourceProps.anchor, {})),
    offset(convertRawProp(context, rawProps, "offset", sourceProps.offset, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNPointAnnotationProps::getDiffPropsImplementationTarget() const {
  return "MLRNPointAnnotation";
}

folly::dynamic MLRNPointAnnotationProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNPointAnnotationProps();
  const MLRNPointAnnotationProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNPointAnnotationProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (id != oldProps->id) {
    result["id"] = id;
  }
    
  if (title != oldProps->title) {
    result["title"] = title;
  }
    
  if (snippet != oldProps->snippet) {
    result["snippet"] = snippet;
  }
    
  if (selected != oldProps->selected) {
    result["selected"] = selected;
  }
    
  if (draggable != oldProps->draggable) {
    result["draggable"] = draggable;
  }
    
  if (lngLat != oldProps->lngLat) {
    result["lngLat"] = lngLat;
  }
    
  if (anchor != oldProps->anchor) {
    result["anchor"] = toDynamic(anchor);
  }
    
  if (offset != oldProps->offset) {
    result["offset"] = toDynamic(offset);
  }
  return result;
}
#endif
MLRNCameraProps::MLRNCameraProps(
    const PropsParserContext &context,
    const MLRNCameraProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    stop(convertRawProp(context, rawProps, "stop", sourceProps.stop, {})),
    initialViewState(convertRawProp(context, rawProps, "initialViewState", sourceProps.initialViewState, {})),
    minZoom(convertRawProp(context, rawProps, "minZoom", sourceProps.minZoom, {-1.0})),
    maxZoom(convertRawProp(context, rawProps, "maxZoom", sourceProps.maxZoom, {-1.0})),
    maxBounds(convertRawProp(context, rawProps, "maxBounds", sourceProps.maxBounds, {})),
    trackUserLocation(convertRawProp(context, rawProps, "trackUserLocation", sourceProps.trackUserLocation, {MLRNCameraTrackUserLocation::None})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNCameraProps::getDiffPropsImplementationTarget() const {
  return "MLRNCamera";
}

folly::dynamic MLRNCameraProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNCameraProps();
  const MLRNCameraProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNCameraProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (stop != oldProps->stop) {
    result["stop"] = toDynamic(stop);
  }
    
  if (initialViewState != oldProps->initialViewState) {
    result["initialViewState"] = toDynamic(initialViewState);
  }
    
  if ((minZoom != oldProps->minZoom) && !(std::isnan(minZoom) && std::isnan(oldProps->minZoom))) {
    result["minZoom"] = minZoom;
  }
    
  if ((maxZoom != oldProps->maxZoom) && !(std::isnan(maxZoom) && std::isnan(oldProps->maxZoom))) {
    result["maxZoom"] = maxZoom;
  }
    
  if (maxBounds != oldProps->maxBounds) {
    result["maxBounds"] = toDynamic(maxBounds);
  }
    
  if (trackUserLocation != oldProps->trackUserLocation) {
    result["trackUserLocation"] = toDynamic(trackUserLocation);
  }
  return result;
}
#endif
MLRNImagesProps::MLRNImagesProps(
    const PropsParserContext &context,
    const MLRNImagesProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    images(convertRawProp(context, rawProps, "images", sourceProps.images, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNImagesProps::getDiffPropsImplementationTarget() const {
  return "MLRNImages";
}

folly::dynamic MLRNImagesProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNImagesProps();
  const MLRNImagesProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNImagesProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (images != oldProps->images) {
    result["images"] = images;
  }
  return result;
}
#endif
MLRNLayerProps::MLRNLayerProps(
    const PropsParserContext &context,
    const MLRNLayerProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    id(convertRawProp(context, rawProps, "id", sourceProps.id, {})),
    layerType(convertRawProp(context, rawProps, "layerType", sourceProps.layerType, {MLRNLayerLayerType::Background})),
    source(convertRawProp(context, rawProps, "source", sourceProps.source, {})),
    sourceLayer(convertRawProp(context, rawProps, "sourceLayer", sourceProps.sourceLayer, {})),
    afterId(convertRawProp(context, rawProps, "afterId", sourceProps.afterId, {})),
    beforeId(convertRawProp(context, rawProps, "beforeId", sourceProps.beforeId, {})),
    layerIndex(convertRawProp(context, rawProps, "layerIndex", sourceProps.layerIndex, {0})),
    minzoom(convertRawProp(context, rawProps, "minzoom", sourceProps.minzoom, {0.0})),
    maxzoom(convertRawProp(context, rawProps, "maxzoom", sourceProps.maxzoom, {0.0})),
    filter(convertRawProp(context, rawProps, "filter", sourceProps.filter, {})),
    reactStyle(convertRawProp(context, rawProps, "reactStyle", sourceProps.reactStyle, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNLayerProps::getDiffPropsImplementationTarget() const {
  return "MLRNLayer";
}

folly::dynamic MLRNLayerProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNLayerProps();
  const MLRNLayerProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNLayerProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (id != oldProps->id) {
    result["id"] = id;
  }
    
  if (layerType != oldProps->layerType) {
    result["layerType"] = toDynamic(layerType);
  }
    
  if (source != oldProps->source) {
    result["source"] = source;
  }
    
  if (sourceLayer != oldProps->sourceLayer) {
    result["sourceLayer"] = sourceLayer;
  }
    
  if (afterId != oldProps->afterId) {
    result["afterId"] = afterId;
  }
    
  if (beforeId != oldProps->beforeId) {
    result["beforeId"] = beforeId;
  }
    
  if (layerIndex != oldProps->layerIndex) {
    result["layerIndex"] = layerIndex;
  }
    
  if ((minzoom != oldProps->minzoom) && !(std::isnan(minzoom) && std::isnan(oldProps->minzoom))) {
    result["minzoom"] = minzoom;
  }
    
  if ((maxzoom != oldProps->maxzoom) && !(std::isnan(maxzoom) && std::isnan(oldProps->maxzoom))) {
    result["maxzoom"] = maxzoom;
  }
    
  if (filter != oldProps->filter) {
    result["filter"] = filter;
  }
    
  if (reactStyle != oldProps->reactStyle) {
    result["reactStyle"] = reactStyle;
  }
  return result;
}
#endif
MLRNAndroidTextureMapViewProps::MLRNAndroidTextureMapViewProps(
    const PropsParserContext &context,
    const MLRNAndroidTextureMapViewProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    mapStyle(convertRawProp(context, rawProps, "mapStyle", sourceProps.mapStyle, {})),
    light(convertRawProp(context, rawProps, "light", sourceProps.light, {})),
    contentInset(convertRawProp(context, rawProps, "contentInset", sourceProps.contentInset, {})),
    preferredFramesPerSecond(convertRawProp(context, rawProps, "preferredFramesPerSecond", sourceProps.preferredFramesPerSecond, {-1})),
    dragPan(convertRawProp(context, rawProps, "dragPan", sourceProps.dragPan, {true})),
    touchZoom(convertRawProp(context, rawProps, "touchZoom", sourceProps.touchZoom, {true})),
    doubleTapZoom(convertRawProp(context, rawProps, "doubleTapZoom", sourceProps.doubleTapZoom, {true})),
    doubleTapHoldZoom(convertRawProp(context, rawProps, "doubleTapHoldZoom", sourceProps.doubleTapHoldZoom, {true})),
    touchRotate(convertRawProp(context, rawProps, "touchRotate", sourceProps.touchRotate, {true})),
    touchPitch(convertRawProp(context, rawProps, "touchPitch", sourceProps.touchPitch, {true})),
    tintColor(convertRawProp(context, rawProps, "tintColor", sourceProps.tintColor, {})),
    attribution(convertRawProp(context, rawProps, "attribution", sourceProps.attribution, {true})),
    attributionPosition(convertRawProp(context, rawProps, "attributionPosition", sourceProps.attributionPosition, {})),
    logo(convertRawProp(context, rawProps, "logo", sourceProps.logo, {true})),
    logoPosition(convertRawProp(context, rawProps, "logoPosition", sourceProps.logoPosition, {})),
    compass(convertRawProp(context, rawProps, "compass", sourceProps.compass, {false})),
    compassPosition(convertRawProp(context, rawProps, "compassPosition", sourceProps.compassPosition, {})),
    compassHiddenFacingNorth(convertRawProp(context, rawProps, "compassHiddenFacingNorth", sourceProps.compassHiddenFacingNorth, {true})),
    scaleBar(convertRawProp(context, rawProps, "scaleBar", sourceProps.scaleBar, {false})),
    scaleBarPosition(convertRawProp(context, rawProps, "scaleBarPosition", sourceProps.scaleBarPosition, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNAndroidTextureMapViewProps::getDiffPropsImplementationTarget() const {
  return "MLRNAndroidTextureMapView";
}

folly::dynamic MLRNAndroidTextureMapViewProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNAndroidTextureMapViewProps();
  const MLRNAndroidTextureMapViewProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNAndroidTextureMapViewProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (mapStyle != oldProps->mapStyle) {
    result["mapStyle"] = mapStyle;
  }
    
  if (light != oldProps->light) {
    result["light"] = light;
  }
    
  if (contentInset != oldProps->contentInset) {
    result["contentInset"] = toDynamic(contentInset);
  }
    
  if (preferredFramesPerSecond != oldProps->preferredFramesPerSecond) {
    result["preferredFramesPerSecond"] = preferredFramesPerSecond;
  }
    
  if (dragPan != oldProps->dragPan) {
    result["dragPan"] = dragPan;
  }
    
  if (touchZoom != oldProps->touchZoom) {
    result["touchZoom"] = touchZoom;
  }
    
  if (doubleTapZoom != oldProps->doubleTapZoom) {
    result["doubleTapZoom"] = doubleTapZoom;
  }
    
  if (doubleTapHoldZoom != oldProps->doubleTapHoldZoom) {
    result["doubleTapHoldZoom"] = doubleTapHoldZoom;
  }
    
  if (touchRotate != oldProps->touchRotate) {
    result["touchRotate"] = touchRotate;
  }
    
  if (touchPitch != oldProps->touchPitch) {
    result["touchPitch"] = touchPitch;
  }
    
  if (tintColor != oldProps->tintColor) {
    result["tintColor"] = *tintColor;
  }
    
  if (attribution != oldProps->attribution) {
    result["attribution"] = attribution;
  }
    
  if (attributionPosition != oldProps->attributionPosition) {
    result["attributionPosition"] = toDynamic(attributionPosition);
  }
    
  if (logo != oldProps->logo) {
    result["logo"] = logo;
  }
    
  if (logoPosition != oldProps->logoPosition) {
    result["logoPosition"] = toDynamic(logoPosition);
  }
    
  if (compass != oldProps->compass) {
    result["compass"] = compass;
  }
    
  if (compassPosition != oldProps->compassPosition) {
    result["compassPosition"] = toDynamic(compassPosition);
  }
    
  if (compassHiddenFacingNorth != oldProps->compassHiddenFacingNorth) {
    result["compassHiddenFacingNorth"] = compassHiddenFacingNorth;
  }
    
  if (scaleBar != oldProps->scaleBar) {
    result["scaleBar"] = scaleBar;
  }
    
  if (scaleBarPosition != oldProps->scaleBarPosition) {
    result["scaleBarPosition"] = toDynamic(scaleBarPosition);
  }
  return result;
}
#endif
MLRNMapViewProps::MLRNMapViewProps(
    const PropsParserContext &context,
    const MLRNMapViewProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    mapStyle(convertRawProp(context, rawProps, "mapStyle", sourceProps.mapStyle, {})),
    light(convertRawProp(context, rawProps, "light", sourceProps.light, {})),
    contentInset(convertRawProp(context, rawProps, "contentInset", sourceProps.contentInset, {})),
    preferredFramesPerSecond(convertRawProp(context, rawProps, "preferredFramesPerSecond", sourceProps.preferredFramesPerSecond, {-1})),
    dragPan(convertRawProp(context, rawProps, "dragPan", sourceProps.dragPan, {true})),
    touchZoom(convertRawProp(context, rawProps, "touchZoom", sourceProps.touchZoom, {true})),
    doubleTapZoom(convertRawProp(context, rawProps, "doubleTapZoom", sourceProps.doubleTapZoom, {true})),
    doubleTapHoldZoom(convertRawProp(context, rawProps, "doubleTapHoldZoom", sourceProps.doubleTapHoldZoom, {true})),
    touchRotate(convertRawProp(context, rawProps, "touchRotate", sourceProps.touchRotate, {true})),
    touchPitch(convertRawProp(context, rawProps, "touchPitch", sourceProps.touchPitch, {true})),
    tintColor(convertRawProp(context, rawProps, "tintColor", sourceProps.tintColor, {})),
    attribution(convertRawProp(context, rawProps, "attribution", sourceProps.attribution, {true})),
    attributionPosition(convertRawProp(context, rawProps, "attributionPosition", sourceProps.attributionPosition, {})),
    logo(convertRawProp(context, rawProps, "logo", sourceProps.logo, {true})),
    logoPosition(convertRawProp(context, rawProps, "logoPosition", sourceProps.logoPosition, {})),
    compass(convertRawProp(context, rawProps, "compass", sourceProps.compass, {false})),
    compassPosition(convertRawProp(context, rawProps, "compassPosition", sourceProps.compassPosition, {})),
    compassHiddenFacingNorth(convertRawProp(context, rawProps, "compassHiddenFacingNorth", sourceProps.compassHiddenFacingNorth, {true})),
    scaleBar(convertRawProp(context, rawProps, "scaleBar", sourceProps.scaleBar, {false})),
    scaleBarPosition(convertRawProp(context, rawProps, "scaleBarPosition", sourceProps.scaleBarPosition, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNMapViewProps::getDiffPropsImplementationTarget() const {
  return "MLRNMapView";
}

folly::dynamic MLRNMapViewProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNMapViewProps();
  const MLRNMapViewProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNMapViewProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (mapStyle != oldProps->mapStyle) {
    result["mapStyle"] = mapStyle;
  }
    
  if (light != oldProps->light) {
    result["light"] = light;
  }
    
  if (contentInset != oldProps->contentInset) {
    result["contentInset"] = toDynamic(contentInset);
  }
    
  if (preferredFramesPerSecond != oldProps->preferredFramesPerSecond) {
    result["preferredFramesPerSecond"] = preferredFramesPerSecond;
  }
    
  if (dragPan != oldProps->dragPan) {
    result["dragPan"] = dragPan;
  }
    
  if (touchZoom != oldProps->touchZoom) {
    result["touchZoom"] = touchZoom;
  }
    
  if (doubleTapZoom != oldProps->doubleTapZoom) {
    result["doubleTapZoom"] = doubleTapZoom;
  }
    
  if (doubleTapHoldZoom != oldProps->doubleTapHoldZoom) {
    result["doubleTapHoldZoom"] = doubleTapHoldZoom;
  }
    
  if (touchRotate != oldProps->touchRotate) {
    result["touchRotate"] = touchRotate;
  }
    
  if (touchPitch != oldProps->touchPitch) {
    result["touchPitch"] = touchPitch;
  }
    
  if (tintColor != oldProps->tintColor) {
    result["tintColor"] = *tintColor;
  }
    
  if (attribution != oldProps->attribution) {
    result["attribution"] = attribution;
  }
    
  if (attributionPosition != oldProps->attributionPosition) {
    result["attributionPosition"] = toDynamic(attributionPosition);
  }
    
  if (logo != oldProps->logo) {
    result["logo"] = logo;
  }
    
  if (logoPosition != oldProps->logoPosition) {
    result["logoPosition"] = toDynamic(logoPosition);
  }
    
  if (compass != oldProps->compass) {
    result["compass"] = compass;
  }
    
  if (compassPosition != oldProps->compassPosition) {
    result["compassPosition"] = toDynamic(compassPosition);
  }
    
  if (compassHiddenFacingNorth != oldProps->compassHiddenFacingNorth) {
    result["compassHiddenFacingNorth"] = compassHiddenFacingNorth;
  }
    
  if (scaleBar != oldProps->scaleBar) {
    result["scaleBar"] = scaleBar;
  }
    
  if (scaleBarPosition != oldProps->scaleBarPosition) {
    result["scaleBarPosition"] = toDynamic(scaleBarPosition);
  }
  return result;
}
#endif
MLRNGeoJSONSourceProps::MLRNGeoJSONSourceProps(
    const PropsParserContext &context,
    const MLRNGeoJSONSourceProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    id(convertRawProp(context, rawProps, "id", sourceProps.id, {})),
    data(convertRawProp(context, rawProps, "data", sourceProps.data, {})),
    maxzoom(convertRawProp(context, rawProps, "maxzoom", sourceProps.maxzoom, {-1})),
    buffer(convertRawProp(context, rawProps, "buffer", sourceProps.buffer, {-1})),
    tolerance(convertRawProp(context, rawProps, "tolerance", sourceProps.tolerance, {-1.0})),
    lineMetrics(convertRawProp(context, rawProps, "lineMetrics", sourceProps.lineMetrics, {false})),
    cluster(convertRawProp(context, rawProps, "cluster", sourceProps.cluster, {false})),
    clusterRadius(convertRawProp(context, rawProps, "clusterRadius", sourceProps.clusterRadius, {-1})),
    clusterMaxZoom(convertRawProp(context, rawProps, "clusterMaxZoom", sourceProps.clusterMaxZoom, {-1})),
    clusterMinPoints(convertRawProp(context, rawProps, "clusterMinPoints", sourceProps.clusterMinPoints, {-1})),
    clusterProperties(convertRawProp(context, rawProps, "clusterProperties", sourceProps.clusterProperties, {})),
    hitbox(convertRawProp(context, rawProps, "hitbox", sourceProps.hitbox, {})),
    hasOnPress(convertRawProp(context, rawProps, "hasOnPress", sourceProps.hasOnPress, {false})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNGeoJSONSourceProps::getDiffPropsImplementationTarget() const {
  return "MLRNGeoJSONSource";
}

folly::dynamic MLRNGeoJSONSourceProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNGeoJSONSourceProps();
  const MLRNGeoJSONSourceProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNGeoJSONSourceProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (id != oldProps->id) {
    result["id"] = id;
  }
    
  if (data != oldProps->data) {
    result["data"] = data;
  }
    
  if (maxzoom != oldProps->maxzoom) {
    result["maxzoom"] = maxzoom;
  }
    
  if (buffer != oldProps->buffer) {
    result["buffer"] = buffer;
  }
    
  if ((tolerance != oldProps->tolerance) && !(std::isnan(tolerance) && std::isnan(oldProps->tolerance))) {
    result["tolerance"] = tolerance;
  }
    
  if (lineMetrics != oldProps->lineMetrics) {
    result["lineMetrics"] = lineMetrics;
  }
    
  if (cluster != oldProps->cluster) {
    result["cluster"] = cluster;
  }
    
  if (clusterRadius != oldProps->clusterRadius) {
    result["clusterRadius"] = clusterRadius;
  }
    
  if (clusterMaxZoom != oldProps->clusterMaxZoom) {
    result["clusterMaxZoom"] = clusterMaxZoom;
  }
    
  if (clusterMinPoints != oldProps->clusterMinPoints) {
    result["clusterMinPoints"] = clusterMinPoints;
  }
    
  if (clusterProperties != oldProps->clusterProperties) {
    result["clusterProperties"] = clusterProperties;
  }
    
  if (hitbox != oldProps->hitbox) {
    result["hitbox"] = toDynamic(hitbox);
  }
    
  if (hasOnPress != oldProps->hasOnPress) {
    result["hasOnPress"] = hasOnPress;
  }
  return result;
}
#endif
MLRNImageSourceProps::MLRNImageSourceProps(
    const PropsParserContext &context,
    const MLRNImageSourceProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    id(convertRawProp(context, rawProps, "id", sourceProps.id, {})),
    url(convertRawProp(context, rawProps, "url", sourceProps.url, {})),
    coordinates(convertRawProp(context, rawProps, "coordinates", sourceProps.coordinates, {})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNImageSourceProps::getDiffPropsImplementationTarget() const {
  return "MLRNImageSource";
}

folly::dynamic MLRNImageSourceProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNImageSourceProps();
  const MLRNImageSourceProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNImageSourceProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (id != oldProps->id) {
    result["id"] = id;
  }
    
  if (url != oldProps->url) {
    result["url"] = url;
  }
    
  if (coordinates != oldProps->coordinates) {
    result["coordinates"] = coordinates;
  }
  return result;
}
#endif
MLRNRasterDEMSourceProps::MLRNRasterDEMSourceProps(
    const PropsParserContext &context,
    const MLRNRasterDEMSourceProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    id(convertRawProp(context, rawProps, "id", sourceProps.id, {})),
    url(convertRawProp(context, rawProps, "url", sourceProps.url, {})),
    tiles(convertRawProp(context, rawProps, "tiles", sourceProps.tiles, {})),
    tileSize(convertRawProp(context, rawProps, "tileSize", sourceProps.tileSize, {512})),
    minzoom(convertRawProp(context, rawProps, "minzoom", sourceProps.minzoom, {-1})),
    maxzoom(convertRawProp(context, rawProps, "maxzoom", sourceProps.maxzoom, {-1})),
    attribution(convertRawProp(context, rawProps, "attribution", sourceProps.attribution, {})),
    encoding(convertRawProp(context, rawProps, "encoding", sourceProps.encoding, {MLRNRasterDEMSourceEncoding::Mapbox})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNRasterDEMSourceProps::getDiffPropsImplementationTarget() const {
  return "MLRNRasterDEMSource";
}

folly::dynamic MLRNRasterDEMSourceProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNRasterDEMSourceProps();
  const MLRNRasterDEMSourceProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNRasterDEMSourceProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (id != oldProps->id) {
    result["id"] = id;
  }
    
  if (url != oldProps->url) {
    result["url"] = url;
  }
    
  if (tiles != oldProps->tiles) {
    result["tiles"] = toDynamic(tiles);
  }
    
  if (tileSize != oldProps->tileSize) {
    result["tileSize"] = tileSize;
  }
    
  if (minzoom != oldProps->minzoom) {
    result["minzoom"] = minzoom;
  }
    
  if (maxzoom != oldProps->maxzoom) {
    result["maxzoom"] = maxzoom;
  }
    
  if (attribution != oldProps->attribution) {
    result["attribution"] = attribution;
  }
    
  if (encoding != oldProps->encoding) {
    result["encoding"] = toDynamic(encoding);
  }
  return result;
}
#endif
MLRNRasterSourceProps::MLRNRasterSourceProps(
    const PropsParserContext &context,
    const MLRNRasterSourceProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    id(convertRawProp(context, rawProps, "id", sourceProps.id, {})),
    url(convertRawProp(context, rawProps, "url", sourceProps.url, {})),
    tiles(convertRawProp(context, rawProps, "tiles", sourceProps.tiles, {})),
    tileSize(convertRawProp(context, rawProps, "tileSize", sourceProps.tileSize, {512})),
    minzoom(convertRawProp(context, rawProps, "minzoom", sourceProps.minzoom, {-1})),
    maxzoom(convertRawProp(context, rawProps, "maxzoom", sourceProps.maxzoom, {-1})),
    attribution(convertRawProp(context, rawProps, "attribution", sourceProps.attribution, {})),
    scheme(convertRawProp(context, rawProps, "scheme", sourceProps.scheme, {MLRNRasterSourceScheme::Xyz})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNRasterSourceProps::getDiffPropsImplementationTarget() const {
  return "MLRNRasterSource";
}

folly::dynamic MLRNRasterSourceProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNRasterSourceProps();
  const MLRNRasterSourceProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNRasterSourceProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (id != oldProps->id) {
    result["id"] = id;
  }
    
  if (url != oldProps->url) {
    result["url"] = url;
  }
    
  if (tiles != oldProps->tiles) {
    result["tiles"] = toDynamic(tiles);
  }
    
  if (tileSize != oldProps->tileSize) {
    result["tileSize"] = tileSize;
  }
    
  if (minzoom != oldProps->minzoom) {
    result["minzoom"] = minzoom;
  }
    
  if (maxzoom != oldProps->maxzoom) {
    result["maxzoom"] = maxzoom;
  }
    
  if (attribution != oldProps->attribution) {
    result["attribution"] = attribution;
  }
    
  if (scheme != oldProps->scheme) {
    result["scheme"] = toDynamic(scheme);
  }
  return result;
}
#endif
MLRNVectorSourceProps::MLRNVectorSourceProps(
    const PropsParserContext &context,
    const MLRNVectorSourceProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    id(convertRawProp(context, rawProps, "id", sourceProps.id, {})),
    url(convertRawProp(context, rawProps, "url", sourceProps.url, {})),
    tiles(convertRawProp(context, rawProps, "tiles", sourceProps.tiles, {})),
    minzoom(convertRawProp(context, rawProps, "minzoom", sourceProps.minzoom, {-1})),
    maxzoom(convertRawProp(context, rawProps, "maxzoom", sourceProps.maxzoom, {-1})),
    scheme(convertRawProp(context, rawProps, "scheme", sourceProps.scheme, {MLRNVectorSourceScheme::Xyz})),
    attribution(convertRawProp(context, rawProps, "attribution", sourceProps.attribution, {})),
    hitbox(convertRawProp(context, rawProps, "hitbox", sourceProps.hitbox, {})),
    hasOnPress(convertRawProp(context, rawProps, "hasOnPress", sourceProps.hasOnPress, {false})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNVectorSourceProps::getDiffPropsImplementationTarget() const {
  return "MLRNVectorSource";
}

folly::dynamic MLRNVectorSourceProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNVectorSourceProps();
  const MLRNVectorSourceProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNVectorSourceProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (id != oldProps->id) {
    result["id"] = id;
  }
    
  if (url != oldProps->url) {
    result["url"] = url;
  }
    
  if (tiles != oldProps->tiles) {
    result["tiles"] = toDynamic(tiles);
  }
    
  if (minzoom != oldProps->minzoom) {
    result["minzoom"] = minzoom;
  }
    
  if (maxzoom != oldProps->maxzoom) {
    result["maxzoom"] = maxzoom;
  }
    
  if (scheme != oldProps->scheme) {
    result["scheme"] = toDynamic(scheme);
  }
    
  if (attribution != oldProps->attribution) {
    result["attribution"] = attribution;
  }
    
  if (hitbox != oldProps->hitbox) {
    result["hitbox"] = toDynamic(hitbox);
  }
    
  if (hasOnPress != oldProps->hasOnPress) {
    result["hasOnPress"] = hasOnPress;
  }
  return result;
}
#endif
MLRNNativeUserLocationProps::MLRNNativeUserLocationProps(
    const PropsParserContext &context,
    const MLRNNativeUserLocationProps &sourceProps,
    const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

    mode(convertRawProp(context, rawProps, "mode", sourceProps.mode, {MLRNNativeUserLocationMode::Default})),
    androidPreferredFramesPerSecond(convertRawProp(context, rawProps, "androidPreferredFramesPerSecond", sourceProps.androidPreferredFramesPerSecond, {-1})) {}
    
#ifdef RN_SERIALIZABLE_STATE
ComponentName MLRNNativeUserLocationProps::getDiffPropsImplementationTarget() const {
  return "MLRNNativeUserLocation";
}

folly::dynamic MLRNNativeUserLocationProps::getDiffProps(
    const Props* prevProps) const {
  static const auto defaultProps = MLRNNativeUserLocationProps();
  const MLRNNativeUserLocationProps* oldProps = prevProps == nullptr
      ? &defaultProps
      : static_cast<const MLRNNativeUserLocationProps*>(prevProps);
  if (this == oldProps) {
    return folly::dynamic::object();
  }
  folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
  
  if (mode != oldProps->mode) {
    result["mode"] = toDynamic(mode);
  }
    
  if (androidPreferredFramesPerSecond != oldProps->androidPreferredFramesPerSecond) {
    result["androidPreferredFramesPerSecond"] = androidPreferredFramesPerSecond;
  }
  return result;
}
#endif

} // namespace facebook::react
